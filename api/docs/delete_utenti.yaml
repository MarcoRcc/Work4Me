/api/utenti/{id}:
  delete:
    summary: Rimozione dell'utente.
    parameters:
      - in: path
        name: id 
        schema:
            type: string
        required: true
        description: l'ID dell'utente.
    responses:
      200:
        description: l'utente e' stato rimosso.
      404:
        description: l'utente non e' stato trovato.
